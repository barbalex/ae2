import React from 'react'

import Router from '../../components/Router'
import Header from '../../components/Head'

const Arten = ({ location }) => <Router location={location} />

export default Arten

export const Head = () => <Header />
